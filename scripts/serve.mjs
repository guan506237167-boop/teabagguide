import http from "node:http";import fs from "node:fs";import path from "node:path";const root=path.resolve("dist");const port=Number(process.env.PORT||4321);const types={".html":"text/html; charset=utf-8",".css":"text/css; charset=utf-8",".js":"application/javascript; charset=utf-8",".svg":"image/svg+xml",".xml":"application/xml; charset=utf-8",".txt":"text/plain; charset=utf-8"};function fileFor(url){const clean=decodeURIComponent(url.split("?")[0]);let file=path.join(root,clean);if(clean.endsWith("/"))file=path.join(root,clean,"index.html");return file}http.createServer((req,res)=>{let file=fileFor(req.url||"/");if(!fs.existsSync(file))file=path.join(root,"index.html");const ext=path.extname(file);res.setHeader("Content-Type",types[ext]||"application/octet-stream");fs.createReadStream(file).pipe(res)}).listen(port,()=>console.log("http://localhost:"+port));